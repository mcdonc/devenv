inputs:
  nixpkgs:
    url: github:mcdonc/nixpkgs/nixpkgs-unstable-python-env-venv-fixed-pkgs
  devenv:
    url: github:mcdonc/devenv/python-rewrite
  overlays:
    url: path:./overlays
    overlays:
      - default
